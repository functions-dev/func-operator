# These resources constitute the fully configured set of manifests
# used to generate the combined 'manifests/' directory in a bundle.
resources:
- ../bases/func-operator.clusterserviceversion.yaml
- ..
- ../../sources/objectbucket/samples
- ../../scorecard
